import {} from "../action/index";

let DEFAULT = {
    area : {},
    map : {},
    event : {}
};

export const mapData = (state = DEFAULT, action) => {
	switch (action.type) {
        default:
            return state;
    }
};