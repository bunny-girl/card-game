import {} from "../action/index";

let DEFAULT = {
	name : "",
	gender : "",
	uid : ""
};

export const userInfo = (state = DEFAULT, action) => {
	switch (action.type) {
        default:
            return state;
    }
};